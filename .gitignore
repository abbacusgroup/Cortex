# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment
.env
.env.local

# OS
.DS_Store
Thumbs.db

# MCP Registry (local publishing artifact)
server.json

# Testing
.pytest_cache/
.coverage
htmlcov/

# uv (lock file committed for reproducibility)

# Session scratch: all per-session plans / findings / test results
# live in ~/Lab/<name>_<DATE>.md (outside the repo) per workspace rule.
# A stray tasks/ directory means something escaped that rule.
tasks/
